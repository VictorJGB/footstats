import { Container } from "./Teams.styles";

type Props = object;

const TeamsPage = (props: Props) => {
  return (
    <Container>
      <h1>Teams page works</h1>
    </Container>
  );
};

export default TeamsPage;
