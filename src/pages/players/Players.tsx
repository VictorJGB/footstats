import { Container } from "./Players.styles";

type Props = object;

const PlayersPage = () => {
  return (
    <Container>
      <h1>PlayersPage page works!!</h1>
    </Container>
  );
};

export default PlayersPage;
